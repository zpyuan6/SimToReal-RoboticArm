base_config: continuous_act_l3only_full_jointtarget.yaml

data_continuous:
  train_path: data/continuous_l3only_jointtarget_tiny/train.npz
  val_path: data/continuous_l3only_jointtarget_tiny/train.npz
  test_path: data/continuous_l3only_jointtarget/test.npz

official_train:
  dataset:
    repo_id: roarm_continuous_train_v2_l3only_jointtarget_tiny
    root: data/lerobot/roarm_continuous_train_v2_l3only_jointtarget_tiny
  steps: 10000
  save_freq: 1000
  output_dir: outputs/train/lerobot/act_v2_l3only_tiny_jointtarget
  job_name: roarm_act_v2_l3only_tiny_jointtarget

early_stop:
  max_steps: 10000
  stage_steps: 1000
  patience: 3
  episodes_per_task: 16
